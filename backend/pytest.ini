[pytest]
# The real unit tests live in tests/. The *_test.py files at the backend root
# are standalone research scripts (they read pickle caches and hit the network),
# not pytest tests — keep discovery scoped to tests/ so `pytest` just works.
testpaths = tests
